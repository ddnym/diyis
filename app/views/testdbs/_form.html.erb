<%= form_for(@testdb) do |f| %>
  <% if @testdb.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@testdb.errors.count, "error") %> prohibited this testdb from being saved:</h2>

      <ul>
      <% @testdb.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
